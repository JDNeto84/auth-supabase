// Exemplo de configuração de ambiente (desenvolvimento)
// Copie este arquivo para `site/js/env-config.js` e substitua pelos valores reais do seu projeto.
// ATENÇÃO: Não comite `site/js/env-config.js` em repositórios públicos.
// Apesar de ser uma KEY publica de acesso anônimo, é recomendado manter este arquivo fora de repositórios públicos para evitar exposição desnecessária.

window.__ENV = {
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'your-anon-key'
};
